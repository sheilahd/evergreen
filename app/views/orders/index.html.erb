<p id="notice"><%= notice %></p>



<h6> The HOA Board of Directors will review and approve your Application Form.</h6> 
<h6> Please Review the documents below and fill out the New Order Form, you can add an image if necessary to support your project </h6>


<p><%= link_to "Application Form", "/order_form.pdf", :target=> "_blank" %></p>
<p><%= link_to "HOA Covenants", "/hoa_covenants.pdf", :target=> "_blank" %></p>
<p><%= link_to "Architectural Form", "/arch_review.pdf", :target=> "_blank" %></p>

<%= link_to 'New Order', new_order_path %>


<% if current_user && current_user.admin%>
          <h1>Orders</h1>

          <table>
              <thead>
                <tr>
                  <th>Name</th>
                  <th>Description</th>
                  <th>Date</th>
                  <th colspan="3"></th>
                </tr>
              </thead>

              <tbody>
                <% @orders.each do |order| %>
                    <tr>
                      <td><%= order.name %></td>
                      <td><%= order.description %></td>
                      <td><%= order.date %></td>
                      
                      <td><%= link_to 'Show', order %></td>
                      <td><%= link_to 'Edit', edit_order_path(order) %></td>
                      <td><%= link_to 'Delete', order, method: :delete, data: { confirm: 'Are you sure?' } %></td>
                    </tr>
                  
              </tbody>
          </table>

              <% end %>
<% end %>





